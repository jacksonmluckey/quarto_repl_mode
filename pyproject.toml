[project]
name = "quarto-repl-mode"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "panflute>=2.3",
    "pygments>=2.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
    "quarto>=0.1.0",
    "ruff>=0.15.1",
]
